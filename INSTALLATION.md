# 📦 دليل التثبيت الشامل - كازينو TN

## المتطلبات الأساسية

- **Node.js** 16 أو أحدث ([تحميل](https://nodejs.org))
- **npm** يأتي مع Node.js
- متصفح حديث (Chrome, Firefox, Safari, Edge)
- اتصال بالإنترنت

---

## التثبيت المحلي

### الخطوة 1: تحميل المشروع

#### الطريقة أ: من GitHub
```bash
git clone https://github.com/yourusername/casino-app.git
cd casino-app
```

#### الطريقة ب: من ZIP
1. حمّل المشروع كـ ZIP
2. فك الضغط
3. افتح Terminal في المجلد

### الخطوة 2: تثبيت المكتبات

```bash
npm install
```

سيتم تثبيت جميع المكتبات تلقائياً من `package.json`

**وقت التثبيت:** 2-5 دقائق (حسب سرعة الإنترنت)

### الخطوة 3: التشغيل

```bash
npm run dev
```

**النتيجة:**
```
VITE v5.0.0 ready in 300 ms

➜ Local:   http://localhost:5173/
➜ press h + enter to show help
```

المتصفح سيفتح تلقائياً على `http://localhost:5173`

---

## التشغيل الأول

### دخول النظام

**اختر أحد الخيارات:**

1. **كـ Admin:**
   - المستخدم: `admin`
   - كلمة السر: `admin123`

2. **إنشاء حساب جديد:**
   - اضغط "ليس لديك حساب؟ إنشاء واحد"
   - ادخل اسم المستخدم والكلمة السرية
   - ستبدأ برصيد 1000 TN

---

## النشر على Netlify

### الطريقة 1: النشر المباشر (الأسرع)

```bash
# تثبيت Netlify CLI
npm install -g netlify-cli

# النشر
netlify deploy
```

متابعة الخطوات:
1. سجل دخول إلى Netlify
2. اختر مشروعاً جديداً أو موجوداً
3. اختر `dist` كمجلد النشر
4. اضغط Deploy

### الطريقة 2: من GitHub (الأفضل للتحديثات)

#### أ. دفع إلى GitHub

```bash
# إنشاء مستودع جديد
git init
git add .
git commit -m "Initial commit"

# دفع إلى GitHub
git remote add origin https://github.com/yourusername/casino-app.git
git push -u origin main
```

#### ب. الربط مع Netlify

1. اذهب إلى [Netlify Dashboard](https://app.netlify.com)
2. اضغط "New site from Git"
3. اختر GitHub
4. اختر المستودع الخاص بك
5. الإعدادات:
   - **Build Command:** `npm run build`
   - **Publish Directory:** `dist`
   - **Node Version:** `16`
6. اضغط Deploy

**النتيجة:** كل push إلى GitHub سينشر تلقائياً! ✨

### الطريقة 3: GitHub Pages

```bash
# بناء المشروع
npm run build

# انسخ محتوى dist إلى gh-pages branch
git add dist
git commit -m "Build for deployment"
git push origin main
```

---

## حل المشاكل الشائعة

### ❌ "npm: command not found"

**الحل:** تثبيت Node.js
```bash
# للتحقق من التثبيت
node --version
npm --version
```

### ❌ "Port 5173 already in use"

**الحل 1:** استخدام port مختلف
```bash
npm run dev -- --port 3000
```

**الحل 2:** إيقاف البرنامج الآخر على نفس الـ port

### ❌ "Cannot find module"

**الحل:**
```bash
# حذف المجلدات والملفات
rm -rf node_modules package-lock.json

# إعادة التثبيت
npm install
```

### ❌ البيانات تختفي عند الإغلاق

**الحل:** تفعيل localStorage
1. افتح Developer Tools (F12)
2. Application → Storage → localStorage
3. تأكد من تفعيل localStorage

### ❌ "Build failed"

**الحل:**
```bash
# تنظيف الـ cache
npm cache clean --force

# إعادة البناء
npm run build
```

---

## الأوامر المتاحة

```bash
# بدء المشروع في بيئة التطوير
npm run dev

# بناء المشروع للإنتاج
npm run build

# عرض معاينة البناء
npm run preview

# حذف المكتبات المثبتة
npm uninstall <package-name>

# تحديث المكتبات
npm update
```

---

## متطلبات الاستضافة

### Netlify
- ✅ مجاني
- ✅ HTTPS مجاني
- ✅ CDN عالمي
- ✅ بناء تلقائي من GitHub
- ✅ دعم متغيرات البيئة
- ⚠️ حد 300 دقيقة بناء/شهر (مجاني)

### GitHub Pages
- ✅ مجاني
- ✅ متصل مع GitHub مباشرة
- ✅ HTTPS مجاني
- ⚠️ أبطأ قليلاً من Netlify

### Vercel
- ✅ مجاني
- ✅ سريع جداً
- ✅ بناء تلقائي
- ✅ Preview URLs للـ PRs

---

## التخصيص قبل النشر

### 1. تغيير الاسم

**الملف:** `index.html`
```html
<title>كازينو TN - اسمك هنا</title>
```

### 2. تحديث الـ favicon

**الملف:** `index.html`
```html
<link rel="icon" type="image/svg+xml" href="/vite.svg" />
```

### 3. تحديث الوصف

**الملف:** `index.html`
```html
<meta name="description" content="وصف تطبيقك هنا" />
```

### 4. إضافة API Key SOFTSWISS (اختياري)

**الملف:** `src/components/GameLobbyEnhanced.jsx`
```javascript
const [apiKey, setApiKey] = useState('YOUR_API_KEY_HERE')
```

---

## إعدادات الأداء

### تحسين الصور

```bash
# استخدام صور معطلة (placeholder)
# أو استخدم خدمات مثل Tinypng.com
```

### تحسين الـ Bundle

```javascript
// vite.config.js
build: {
  rollupOptions: {
    output: {
      manualChunks: {
        'react-vendor': ['react', 'react-dom'],
      }
    }
  }
}
```

---

## الأمان والخصوصية

⚠️ **ملاحظة مهمة:**

هذا التطبيق للأغراض التعليمية والتجريبية فقط. للإنتاج:

- ✅ استخدم Supabase/Firebase للمصادقة
- ✅ لا تخزن كلمات السر كنصوص عادية
- ✅ استخدم HTTPS فقط
- ✅ طبق التحقق من الطلب على الخادم
- ✅ استخدم متغيرات البيئة للـ API Keys

---

## الصيانة والتحديثات

### تحديث المكتبات

```bash
# عرض المكتبات القديمة
npm outdated

# تحديث جميع المكتبات
npm update

# تحديث مكتبة محددة
npm update <package-name>
```

### النسخ الاحتياطية

```bash
# إنشاء نسخة احتياطية
git tag -a v1.0.0 -m "Version 1.0.0"
git push origin v1.0.0
```

---

## المساعدة والدعم

### التحقق من الإصدارات

```bash
node --version
npm --version
```

### معلومات المشروع

```bash
npm list
npm info react
```

### الـ Logs

```bash
# في Netlify
# Settings → Build & deploy → Logs

# في GitHub Pages
# Settings → Pages → Check deployment status
```

---

## الخطوات التالية

بعد التثبيت والنشر:

1. ✅ قم بتخصيص الألوان والتصميم
2. ✅ أضف ألعابك الخاصة
3. ✅ اختبر على أجهزة مختلفة
4. ✅ تحسين الأداء
5. ✅ تفعيل SOFTSWISS API
6. ✅ إضافة قاعدة بيانات حقيقية
7. ✅ تكامل نظام الدفع
8. ✅ نشر النسخة النهائية

---

## عن المشروع

- **الإصدار:** 1.0.0
- **الحالة:** جاهز للإنتاج
- **الترخيص:** مفتوح المصدر
- **آخر تحديث:** يناير 2025

---

**تم! تطبيقك جاهز للاستخدام. استمتع بـ كازينو TN! 🎰**

إذا واجهت أي مشاكل، راجع [DEVELOPMENT_GUIDE.md](./DEVELOPMENT_GUIDE.md) و [CASINO_README.md](./CASINO_README.md)
