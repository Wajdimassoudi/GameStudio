# 🚀 البدء السريع مع كازينو TN

## 3 خطوات سهلة فقط!

### 1️⃣ التثبيت
```bash
npm install
```

### 2️⃣ التشغيل
```bash
npm run dev
```

المتصفح سيفتح تلقائياً على: **http://localhost:5173**

### 3️⃣ تسجيل الدخول
استخدم أحد الخيارات:

**كـ Admin:**
- المستخدم: `admin`
- كلمة السر: `admin123`

**أو إنشاء حساب جديد:**
- اضغط "ليس لديك حساب؟ إنشاء واحد"
- أدخل أي اسم مستخدم
- أدخل كلمة سر (4 أحرف على الأقل)

---

## 🎮 ماذا بعد الدخول؟

### إذا كنت لاعب عادي:
- استعرض قائمة الألعاب
- اختر أي لعبة
- ضع رهان وابدأ اللعب!
- اضغط "+ 500 TN" لإضافة أموال تجريبية

### إذا دخلت كـ Admin:
- **تبويب المستخدمون** - عرض وإدارة جميع اللاعبين
- **تبويب الإدارة** - إنشاء مستخدمين جدد والتحكم في النظام
- **تبويب المعاملات** - عرض سجل جميع المعاملات

---

## 📦 النشر على Netlify

### الخيار 1: عبر الأوامر
```bash
# تثبيت Netlify CLI
npm install -g netlify-cli

# النشر
netlify deploy
```

### الخيار 2: عبر GitHub (الأفضل)
```bash
# دفع الكود إلى GitHub
git add .
git commit -m "Initial commit"
git push origin main

# ثم انتقل إلى Netlify Dashboard
# اختر "Connect from Git"
# اختر Repository الخاص بك
# سيتم النشر تلقائياً مع كل Push
```

### إعدادات Netlify المهمة:
```
Build Command: npm run build
Publish Directory: dist
Node Version: 16 أو أحدث
```

---

## 🔧 المشاكل الشائعة

### ❌ "Command not found: npm"
**الحل:** تثبيت Node.js من https://nodejs.org

### ❌ "Cannot find module 'react'"
**الحل:**
```bash
rm -rf node_modules package-lock.json
npm install
```

### ❌ "Port 5173 already in use"
**الحل:**
```bash
# استخدم port مختلف
npm run dev -- --port 3000
```

### ❌ البيانات تختفي عند الإغلاق
**الحل:** تأكد من تفعيل localStorage في المتصفح
- Firefox/Chrome: Settings → Privacy → cookies/storage

---

## 📝 الملفات المهمة

| الملف | الوصف |
|------|-------|
| `src/App.jsx` | المكون الرئيسي |
| `src/components/` | جميع المكونات |
| `src/utils/storage.js` | نظام التخزين |
| `vite.config.js` | إعدادات Vite |
| `tailwind.config.js` | إعدادات Tailwind |
| `index.html` | صفحة HTML الرئيسية |

---

## 🌐 الوظائف الأساسية

| الوظيفة | الكود |
|-------|------|
| تسجيل دخول | `StorageService.findUser(username, password)` |
| تسجيل مستخدم | `StorageService.addUser(username, password)` |
| تحديث الرصيد | `StorageService.updateBalance(userId, amount)` |
| الحصول على المستخدمين | `StorageService.getAllUsers()` |
| الحصول على المعاملات | `StorageService.getTransactions()` |

---

## 💡 نصائح

1. **استخدم F12** لفتح Developer Console لرؤية الأخطاء
2. **امسح الـ Console** باستخدام `console.clear()`
3. **أعد تعيين البيانات** بكتابة `StorageService.resetData()`
4. **استخدم Tab للتنقل** بين الحقول في النماذج

---

## 📚 مراجع مفيدة

- [React Docs](https://react.dev) - التوثيق الرسمية لـ React
- [Vite Docs](https://vitejs.dev) - توثيق أداة Vite
- [Tailwind CSS](https://tailwindcss.com) - توثيق Tailwind CSS
- [Netlify Docs](https://docs.netlify.com) - توثيق Netlify
- [MDN Web Docs](https://developer.mozilla.org) - مرجع الويب الشامل

---

## ✨ التالي؟

بعد النشر على Netlify، يمكنك:

1. **تخصيص النطاق** - استخدم نطاقك الخاص
2. **إضافة SSL** - Netlify توفره مجاناً
3. **ربط Supabase** - للبيانات السحابية
4. **تكامل الدفع** - Stripe للمدفوعات الحقيقية
5. **تحسين الأداء** - CDN عالمي من Netlify

---

## 🆘 تحتاج مساعدة؟

1. اقرأ [CASINO_README.md](./CASINO_README.md) للمزيد من التفاصيل
2. افتح Console في المتصفح (F12) وابحث عن الأخطاء
3. تحقق من اتصال الإنترنت
4. حاول في متصفح مختلف

---

**استمتع بـ كازينو TN! 🎰**
