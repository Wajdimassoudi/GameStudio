# 🎰 كازينو TN - تطبيق الألعاب الافتراضية

موقع كازينو متكامل بـ React + Vite + Tailwind CSS مع نظام إدارة كامل ودعم SOFTSWISS Game Aggregator.

## المميزات الرئيسية

✅ **نظام مصادقة متكامل** - تسجيل دخول وتسجيل مستخدمين جدد
✅ **لوحة تحكم Admin** - إدارة المستخدمين والرصيد والمعاملات
✅ **نظام عملات افتراضية** - عملة TN للنظام كله
✅ **لوبي ألعاب** - عرض شبكي (3xN) للألعاب
✅ **نظام اللعب** - محاكاة دورات عشوائية مع فرص فوز
✅ **تخزين داخلي** - localStorage للبيانات والمستخدمين
✅ **Responsive Design** - تصميم متجاوب للموبايل والسطح المكتب
✅ **جاهز للنشر** - يعمل على Netlify مباشرة

## البدء السريع

### المتطلبات
- Node.js 16+ و npm

### التثبيت والتشغيل

```bash
# 1. استنساخ أو تحميل المشروع
cd casino-app

# 2. تثبيت المكتبات
npm install

# 3. تشغيل التطوير
npm run dev

# 4. بناء المشروع للنشر
npm run build

# 5. عرض معاينة البناء
npm run preview
```

سيفتح المتصفح تلقائياً على `http://localhost:5173`

## حسابات الاختبار

### حساب Admin
```
المستخدم: admin
كلمة السر: admin123
```

**الصلاحيات:**
- عرض وإدارة جميع المستخدمين
- إضافة وسحب الأموال من حسابات المستخدمين
- إنشاء مستخدمين جدد
- عرض سجل المعاملات الكامل

### إنشاء حساب عادي
اختر "ليس لديك حساب؟ إنشاء واحد" من صفحة الدخول وأدخل:
- اسم مستخدم (أي قيمة)
- كلمة سر (4 أحرف على الأقل)

كل مستخدم جديد يبدأ برصيد **1000 TN**

## هيكل المشروع

```
casino-app/
├── index.html                 # صفحة HTML الرئيسية
├── src/
│   ├── App.jsx               # المكون الرئيسي
│   ├── main.jsx              # نقطة الدخول
│   ├── index.css             # أنماط Tailwind
│   ├── components/
│   │   ├── Login.jsx         # صفحة تسجيل الدخول
│   │   ├── GameLobby.jsx     # لوبي الألعاب
│   │   ├── GameCard.jsx      # بطاقة اللعبة
│   │   ├── GameModal.jsx     # نافذة اللعبة
│   │   └── AdminDashboard.jsx # لوحة تحكم Admin
│   └── utils/
│       └── storage.js        # خدمة التخزين الداخلي
├── vite.config.js            # إعدادات Vite
├── tailwind.config.js        # إعدادات Tailwind
├── postcss.config.js         # إعدادات PostCSS
└── package.json              # المكتبات والسكريبتات
```

## استخدام التطبيق

### للاعبين
1. **تسجيل الدخول** - استخدم حساب موجود أو أنشئ واحداً جديداً
2. **عرض الألعاب** - استعرض لوبي الألعاب والبحث حسب الاسم/المزود
3. **لعب الألعاب** - اختر لعبة وضع رهان لتبدأ
4. **إدارة الرصيد** - اضغط "+ 500 TN" لإضافة أموال تجريبية

### للـ Admin
1. **تسجيل الدخول** - استخدم حساب admin (admin/admin123)
2. **تبويب المستخدمون** - عرض وإدارة جميع المستخدمين
3. **تبويب الإدارة** - إنشاء مستخدمين جدد وعرض معلومات النظام
4. **تبويب المعاملات** - عرض سجل جميع عمليات الإيداع والسحب

## نظام العملات

- **العملة:** TN (Token Coins)
- **الرصيد الافتراضي:** 1000 TN
- **رصيد Admin:** 999999 TN
- **العمليات:**
  - إضافة أموال: + المبلغ
  - سحب أموال: - المبلغ
  - الفوز في اللعبة: + الأرباح

## الألعاب المتوفرة

- Book of Ra
- Starburst
- Gonzo Quest
- Dead or Alive
- Twin Spin
- Aloha Cluster Pays
- Piggy Riches
- South Park
- Immortal Romance

يمكن توسيع قائمة الألعاب بسهولة من خلال تعديل بيانات `mockGames` في `GameLobby.jsx`

## التكامل مع SOFTSWISS

الكود مجهز للتكامل مع SOFTSWISS Game Aggregator:

```javascript
// في GameLobby.jsx - استبدل mockGames بـ API الحقيقي:
const response = await fetch('https://api.softswiss.com/v1/games', {
  headers: {
    'Authorization': `Bearer YOUR_API_KEY`
  }
})
const games = await response.json()
setGames(games)
```

## النشر على Netlify

### الخطوة 1: بناء المشروع
```bash
npm run build
```

### الخطوة 2: رفع على GitHub (اختياري)
```bash
git init
git add .
git commit -m "Initial commit"
git push origin main
```

### الخطوة 3: النشر
```bash
# استخدم Netlify CLI
npm install -g netlify-cli
netlify deploy

# أو اسحب من GitHub على Dashboard Netlify
```

## متطلبات Netlify

- **Build Command:** `npm run build`
- **Publish Directory:** `dist`
- **Node Version:** 16 أو أحدث

## الحفظ والبيانات

جميع البيانات تُحفظ محلياً في `localStorage`:
- بيانات المستخدمين
- الأرصدة والمعاملات
- حالة التطبيق

**ملاحظة:** البيانات تُمسح عند مسح سجل المتصفح. لإعادة تعيين البيانات:
```javascript
// في Console
StorageService.resetData()
```

## إعادة التعيين

لإعادة تعيين النظام إلى حالته الافتراضية:
```javascript
// افتح Developer Console (F12)
// اذهب إلى localStorage
// احذف المفتاح: casino_data_tn
// أو استخدم:
StorageService.resetData()
```

## التخصيص

### تغيير ألوان النظام
عدّل `theme` في `tailwind.config.js`:
```javascript
colors: {
  primary: '#1f2937',    // اللون الأساسي
  secondary: '#4f46e5',  // اللون الثانوي
  accent: '#f59e0b',     // لون البروز
}
```

### تغيير اسم العملة
ابحث عن `TN` في الملفات واستبدله باسمك المفضل

### إضافة ألعاب جديدة
أضف إلى مصفوفة `mockGames` في `GameLobby.jsx`:
```javascript
{
  id: 'game_010',
  title: 'اسم اللعبة',
  provider: 'SOFTSWISS',
  thumbnail: 'رابط الصورة'
}
```

## استكشاف الأخطاء

### المشكلة: لا تظهر الألعاب
- تحقق من اتصال الإنترنت
- افتح Console (F12) وابحث عن الأخطاء

### المشكلة: البيانات تُفقد عند الإغلاق
- تأكد من أن `localStorage` مفعّل في المتصفح
- جرب متصفح آخر

### المشكلة: لا تعمل صور اللعبة
- سيتم استخدام صور placeholder تلقائياً
- تحقق من أن رابط الصورة صحيح

## الأداء والتحسينات

- استخدام Vite للبناء السريع
- Tailwind CSS للأنماط الخفيفة الوزن
- localStorage للتخزين السريع
- كود معوّض (Minified) في الإنتاج

## التطوير المستقبلي

- [ ] تكامل حقيقي مع SOFTSWISS API
- [ ] تكامل Supabase للبيانات السحابية
- [ ] نظام دفع حقيقي (Stripe)
- [ ] تنبيهات في الوقت الفعلي
- [ ] لعبة حقيقية بدل المحاكاة
- [ ] رسوميات ثلاثية الأبعاد
- [ ] نظام رتب اللاعبين
- [ ] تكامل وسائل التواصل الاجتماعي

## الأمان

**ملاحظة مهمة:** هذا تطبيق تجريبي. في البيئة الإنتاجية:
- استخدم Supabase/Firebase للمصادقة الآمنة
- لا تخزن كلمات السر بشكل نصي
- استخدم HTTPS فقط
- طبق التحقق من الطلب على الخادم

## الترخيص

مشروع مفتوح المصدر للأغراض التعليمية

## الدعم والمساعدة

للمزيد من المساعدة:
1. تحقق من التعليقات في الكود
2. راجع التوثيق الرسمية:
   - [React Docs](https://react.dev)
   - [Vite Docs](https://vitejs.dev)
   - [Tailwind Docs](https://tailwindcss.com)
   - [Netlify Docs](https://docs.netlify.com)

---

**تم الإنشاء:** يناير 2025
**النسخة:** 1.0.0
**الحالة:** جاهز للنشر
